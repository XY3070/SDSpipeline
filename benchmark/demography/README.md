# Demography Helpers

This directory stores reusable demography benchmark and evaluation helpers that are part of the canonical SDS pipeline.

It should contain:

- stable Python helpers
- small model registries
- plotting/evaluation utilities

It should not contain:

- large VCF subsets
- run-specific outputs
- queue logs

Those belong in `SDSworkspace/`.

